[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback: serve index.html for client-side routes so deep links don't 404
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
