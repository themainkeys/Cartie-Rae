[build]
  command   = "npm run build"
  publish   = "dist"

# SPA fallback — serve index.html for all routes so client-side navigation works
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
